# ai_agent_consensus_simulator
Một web script/simulator dùng JS + mock AI agents để simulate consensus mechanisms (e.g., feature-based consensus) – user input parameters (nodes, faults) → show process step-by-step with animation, output success rate. Add Endless cloud integration mock.
